<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Concert Section</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-[#0b0215] text-white min-h-screen flex flex-col">
     {{> navBar
     }}
  <div class="flex-1 flex items-center justify-center p-6 mt-20">
  <div class="max-w-sm w-full bg-white/10 backdrop-blur-md border border-white/20 shadow-2xl rounded-2xl overflow-hidden transform hover:scale-[1.02] transition-all duration-300">
    <!-- Ticket Header -->
    <div class="bg-[#2a0050] text-white text-center py-4 border-b border-white/10">
      <h1 class="text-2xl font-semibold tracking-wide">{{booking.concertId.name}}</h1>
      <p class="text-sm opacity-80">Concert place</p>
    </div>

    <!-- Ticket Body -->
    <div class="p-6 text-gray-200">
      <ul class="mb-6">
        <li class="text-sm mb-2">
          <span class="font-semibold">Reference :</span> #{{booking._id}}
        </li>
        <li class="text-sm mb-2">
          <span class="font-semibold">Tickets :</span> {{booking.ticketsCount}}
        </li>
        <li class="text-sm mb-2">
          <span class="font-semibold">Status :</span> {{booking.status}}
        </li>
      </ul>

      <div class="flex justify-between text-sm text-gray-300 mb-4">
        <div>
          <p class="font-semibold">Artist :</p>
          <p>{{booking.concertId.band_id.name}}</p>
        </div>
        <div>
          <p class="font-semibold">Date :</p>
          <p>June 1, 2025</p>
        </div>
      </div>

      <div class="flex justify-between text-sm text-gray-300 mb-4">
        <div>
          <p class="font-semibold">Hour :</p>
          <p>{{booking.bookingDate}}</p>
        </div>
        <div>
          <p class="font-semibold">Places :</p>
          <p>{{booking.ticketsCount}}</p>
        </div>
      </div>

      <!-- QR Code -->
      <div class="text-center mt-8">
        <img src="{{booking.qrCode}}" alt="QR Code" width="200" class="mx-auto mb-4 rounded-lg shadow-md border border-white/20">
        <p class="text-xs text-gray-400 italic">Scan to enter</p>
      </div>
    </div>

    <!-- Ticket Footer -->
    <div class="bg-[#2a0050] text-white text-center py-3 border-t border-white/10">
      <p class="text-sm tracking-wide">
        ##############
      </p>
    </div>
    </div>
  </div>
  <script>
    console.log({{booking}})
  </script>
{{> footer
     }}
  </body>
</html>