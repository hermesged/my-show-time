<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#0b0215] text-white min-h-screen flex">

  {{> asideAdmin
     }}

  <div class="flex-1 flex flex-col">

    <header class="flex justify-end items-center bg-[#141020] p-6 border-b border-fuchsia-700/20">
      <span class="font-semibold text-gray-300">
        admin {{currentAdmin.name}}
      </span>
    </header>

    <!-- Form Section -->
    <main class="flex-1 p-8">
      <div class="max-w-xl mx-auto bg-[#1f1b38] p-8 rounded-lg shadow-lg">
        <h2 class="text-2xl font-bold mb-6 text-white">Add New User</h2>

        {{#if errors}}
          <div class="bg-red-700/40 text-red-200 p-4 mb-4 rounded">
            <ul>
              {{#each errors}}
                <li>{{this}}</li>
              {{/each}}
            </ul>
          </div>
        {{/if}}

        <form action="/users/admin" method="POST" class="flex flex-col gap-4">
          <div>
            <label class="block mb-1 font-semibold" for="name">Name</label>
            <input type="text" name="name" id="name" class="w-full p-2 rounded bg-[#12052b] border border-fuchsia-700/20 text-white" required>
          </div>

          <div>
            <label class="block mb-1 font-semibold" for="email">Email</label>
            <input type="email" name="email" id="email" class="w-full p-2 rounded bg-[#12052b] border border-fuchsia-700/20 text-white" required>
          </div>

          <div>
            <label class="block mb-1 font-semibold" for="password">Password</label>
            <input type="password" name="password" id="password" class="w-full p-2 rounded bg-[#12052b] border border-fuchsia-700/20 text-white" required>
          </div>

          <div>
            <label class="block mb-1 font-semibold" for="role">Role</label>
            <select name="role" id="role" class="w-full p-2 rounded bg-[#12052b] border border-fuchsia-700/20 text-white">
              <option value="user">User</option>
              <option value="admin">Admin</option>
            </select>
          </div>

          <button type="submit" class="mt-4 bg-fuchsia-600 hover:bg-fuchsia-700 text-white font-semibold py-2 px-4 rounded transition-all duration-200">
            Add User
          </button>
        </form>
      </div>
    </main>
  </div>

</body>
</html>
