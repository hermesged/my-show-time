<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#0b0215] text-white min-h-screen flex">

  {{> asideAdmin
     }}

  <div class="flex-1 flex flex-col">

    <header class="flex justify-end items-center bg-[#141020] p-6 border-b border-fuchsia-700/20">
      <span class="font-semibold text-gray-300">
        admin {{currentAdmin.name}}
      </span>
    </header>


    <main class="p-6">
      <h2 class="text-2xl font-bold mb-6 text-gray-100">Edit User</h2>

      {{#if errors}}
        <div class="bg-red-700/30 text-red-200 p-4 mb-4 rounded">
          <ul>
            {{#each errors}}
              <li>{{this}}</li>
            {{/each}}
          </ul>
        </div>
      {{/if}}

      {{#if message}}
        <div class="bg-green-700/30 text-green-200 p-4 mb-4 rounded">
          {{message}}
        </div>
      {{/if}}

      <form action="/users/{{user._id}}" method="POST" class="bg-[#1b1035] p-6 rounded-lg shadow-lg max-w-lg">
        <input type="hidden" name="_method" value="PATCH">

        <div class="mb-4">
          <label for="name" class="block text-gray-300 font-semibold mb-1">Name</label>
          <input type="text" id="name" name="name" value="{{user.name}}" class="w-full px-3 py-2 rounded bg-[#12052b] text-gray-200 border border-fuchsia-700/20 focus:outline-none focus:ring-2 focus:ring-fuchsia-500">
        </div>

        <div class="mb-4">
          <label for="email" class="block text-gray-300 font-semibold mb-1">Email</label>
          <input type="email" id="email" name="email" value="{{user.email}}" class="w-full px-3 py-2 rounded bg-[#12052b] text-gray-200 border border-fuchsia-700/20 focus:outline-none focus:ring-2 focus:ring-fuchsia-500">
        </div>

        <div class="mb-4">
          <label for="role" class="block text-gray-300 font-semibold mb-1">Role</label>
          <select id="role" name="role" class="w-full px-3 py-2 rounded bg-[#12052b] text-gray-200 border border-fuchsia-700/20 focus:outline-none focus:ring-2 focus:ring-fuchsia-500">
           <option value="user" {{#ifCond user.role 'user'}}selected{{/ifCond}}>User</option>
            <option value="admin" {{#ifCond user.role 'admin'}}selected{{/ifCond}}>Admin</option>
          </select>
        </div>

        <button type="submit" class="px-6 py-2 bg-fuchsia-600 hover:bg-fuchsia-700 text-white font-semibold rounded transition-colors">
          Save Changes
        </button>
      </form>
    </main>
  </div>

</body>
</html>
