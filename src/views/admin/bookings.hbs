<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#0b0215] text-white min-h-screen flex">

  {{> asideAdmin
     }}

  <div class="flex-1 flex flex-col">
    
    <header class="flex justify-end items-center bg-[#141020] p-6 border-b border-fuchsia-700/20">
      <span class="font-semibold text-gray-300">
        admin {{currentAdmin.name}}
      </span>
    </header>

    <div class="max-w-6xl mx-auto">
      <div class="flex justify-between items-center mb-8">
        <h1 class="text-3xl font-bold text-white">All Tickets</h1>
        <a href="/bookings/new"
          class="px-4 py-2 bg-[#A259FF] hover:bg-[#8b3eff] rounded-lg text-white text-sm font-medium transition">
          + Add Ticket
        </a>
      </div>

      <div class="overflow-x-auto">
        <table class="w-full text-sm text-left text-gray-300 border border-gray-700 rounded-lg overflow-hidden">
          <thead class="bg-[#1a0b2e] text-gray-200 uppercase text-xs tracking-wider">
            <tr>
              <th scope="col" class="px-6 py-3">Ref</th>
              <th scope="col" class="px-6 py-3">User ID</th>
              <th scope="col" class="px-6 py-3">Concert</th>
              <th scope="col" class="px-6 py-3">Tickets</th>
              <th scope="col" class="px-6 py-3">Status</th>
              <th scope="col" class="px-6 py-3">QR Code</th>
              <th scope="col" class="px-6 py-3 text-right">Actions</th>
            </tr>
          </thead>

          <tbody>
            {{#each bookings}}
            <tr class="border-t border-gray-700 hover:bg-[#1a0b2e]/40 transition">
              <td class="px-6 py-4 font-mono text-purple-400">#{{_id}}</td>
              <td class="px-6 py-4">{{userId}}</td>
              <td class="px-6 py-4">{{concertId.name}}</td>
              <td class="px-6 py-4">{{ticketsCount}}</td>
              <td class="px-6 py-4">
               <span class="px-2 py-1 rounded-md text-xs bg-gray-700/50">
                {{status}}
                </span>
              </td>
              <td class="px-6 py-4">
                <img src="{{qrCode}}" alt="QR" width="60" class="rounded-md border border-gray-600" />
              </td>
              <td class="px-6 py-4 text-right space-x-2">
                <a href="/bookings/{{_id}}/edit"
                  class="px-3 py-1 bg-blue-600 hover:bg-blue-500 rounded text-white text-xs transition">Edit</a>

                <form action="/bookings/{{_id}}/delete" method="POST" class="inline" onsubmit="return confirm('Delete this ticket?')">
                  <input type="hidden" name="_method" value="DELETE" />
                  <button type="submit"
                    class="px-3 py-1 bg-red-600/70 hover:bg-red-500 rounded text-white text-xs transition">
                    Delete
                  </button>
                </form>
              </td>
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>

  </body>
</html>
